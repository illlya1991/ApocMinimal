<Window x:Class="ApocMinimal.NewGameSetupWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Новая игра"
        Height="580" Width="520"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#0d0d1a">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto" Padding="32,24,32,8">
        <StackPanel>

            <TextBlock Text="НАСТРОЙКА НОВОГО МИРА"
                       Foreground="#8b949e"
                       FontSize="11" FontWeight="Bold"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,24"/>

            <!-- Player name -->
            <TextBlock Text="Имя игрока"
                       Foreground="#6b7280"
                       FontSize="11"
                       Margin="0,0,0,6"/>
            <TextBox x:Name="NameBox"
                     Height="36" FontSize="14"
                     Background="#161b22" Foreground="#e0e0e0"
                     BorderBrush="#30363d" BorderThickness="1"
                     Padding="8,0"
                     CaretBrush="White"
                     VerticalContentAlignment="Center"
                     MaxLength="24"
                     Margin="0,0,0,24"/>

            <!-- Faction -->
            <TextBlock Text="Фракция"
                       Foreground="#6b7280"
                       FontSize="11"
                       Margin="0,0,0,6"/>
            <ListBox x:Name="FactionList"
                     Height="160"
                     Background="#161b22"
                     BorderBrush="#30363d"
                     BorderThickness="1"
                     SelectionChanged="FactionList_SelectionChanged"
                     Margin="0,0,0,10"/>

            <Border x:Name="DescBorder"
                    Background="#161b22" CornerRadius="4"
                    Padding="10,8" Margin="0,0,0,24"
                    MinHeight="42">
                <TextBlock x:Name="FactionDescText"
                           FontSize="11" TextWrapping="Wrap"
                           Foreground="#8b949e"/>
            </Border>

        </StackPanel>
        </ScrollViewer>

        <!-- Buttons always at bottom -->
        <Border Grid.Row="1" Background="#0d0d1a" Padding="32,10,32,20"
                BorderBrush="#21262d" BorderThickness="0,1,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="12"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Button Grid.Column="0"
                        Content="ОТМЕНА"
                        Click="Cancel_Click"
                        Height="38" FontSize="12"
                        Foreground="#8b949e" Background="#21262d"
                        BorderBrush="#30363d" Cursor="Hand"/>
                <Button Grid.Column="2"
                        x:Name="ConfirmBtn"
                        Content="НАЧАТЬ ИГРУ"
                        Click="Confirm_Click"
                        Height="38" FontSize="12" FontWeight="Bold"
                        Foreground="White" Background="#4a90d9"
                        BorderThickness="0" Cursor="Hand"/>
            </Grid>
        </Border>

    </Grid>
</Window>
